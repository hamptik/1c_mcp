# Конфигурация MCP-прокси для Docker

# Настройки подключения к 1С (обязательные)
# ВАЖНО: Если 1С на том же хосте, используйте host.docker.internal вместо localhost
# Для Linux: используйте IP хоста (например, 172.17.0.1) или запустите с --network host
MCP_ONEC_URL=http://host.docker.internal/your_base_name
MCP_ONEC_USERNAME=your_username
MCP_ONEC_PASSWORD=your_password

# Настройки HTTP-сервиса 1С (опциональные)
MCP_ONEC_SERVICE_ROOT=mcp

# Настройки HTTP-сервера (опциональные)
MCP_HOST=0.0.0.0
MCP_PORT=8000

# Настройки MCP-сервера (опциональные)
MCP_SERVER_NAME=1C-MCP-Proxy
MCP_SERVER_VERSION=1.0.0

# Настройки логирования (опциональные)
MCP_LOG_LEVEL=INFO

# Настройки авторизации OAuth2 (опциональные)
MCP_AUTH_MODE=none

# Публичный URL прокси для OAuth2 (обязательно при AUTH_MODE=oauth2)
# MCP_PUBLIC_URL=https://your-mcp-proxy.example.com

# Доверенные IP прокси для X-Forwarded-* заголовков (по умолчанию: 127.0.0.1)
# При работе за reverse proxy/внутри docker укажите IP прокси или "*" для доверия ко всем
MCP_FORWARDED_ALLOW_IPS=*

# TTL для OAuth2 токенов (опциональные)
MCP_OAUTH2_CODE_TTL=120
MCP_OAUTH2_ACCESS_TTL=3600
MCP_OAUTH2_REFRESH_TTL=1209600

# Хранилище OAuth2 данных (опциональные)
# Backend: sqlite (персистентный, по умолчанию) или memory (только RAM)
# MCP_STORAGE_BACKEND=sqlite
# Путь к файлу SQLite (по умолчанию: data/oauth2.db)
# MCP_STORAGE_PATH=data/oauth2.db
